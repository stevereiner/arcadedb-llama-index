# Core dependencies
# ArcadeDB Python client (now available on PyPI)
arcadedb-python>=0.4.0

# LlamaIndex core components
llama-index-core>=0.13.0

# Optional: LlamaIndex integrations for examples
llama-index-embeddings-openai
llama-index-llms-openai

# Optional: embedded ArcadeDB (in-process JVM via JPype, ~95MB installed)
# Includes bundled Java 25 JRE (~60MB) + ArcadeDB JARs (~32MB) + Python bindings
# Only needed when using mode="embedded" in ArcadeDBPropertyGraphStore
# Install with: uv pip install arcadedb-embedded
# arcadedb-embedded>=26.3.2

# Development dependencies (install with: uv pip install -e ".[dev]")
# black>=23.0.0
# mypy>=1.0.0
# pre-commit>=3.0.0
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
# ruff>=0.1.0
